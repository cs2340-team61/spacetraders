<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.gatech.cs2340">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PirateActivity"></activity>
        <activity android:name=".TravelActivity" />
        <activity android:name=".ShipInventoryActivity" />
        <activity android:name=".GameMainActivity" />
        <activity android:name=".MarketSellActivity" />
        <activity android:name=".MarketBuyActivity" />
        <activity android:name=".MarketplaceActivity" />
        <activity android:name=".GameStartActivity" />
        <activity android:name=".GameConfigActivity" />
        <activity
            android:name=".PostConfigActivity"
            android:label="@string/title_activity_post_config"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>